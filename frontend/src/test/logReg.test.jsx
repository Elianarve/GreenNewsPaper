import { registerUser, loginUser } from '../services/logReg';

jest.mock('axios');

describe('registerUser', () => {
  it('should register user successfully', async () => {
    // Mock axios response
  });

  it('should throw error if token is not provided in response', async () => {
    // Mock axios response
  });
});
